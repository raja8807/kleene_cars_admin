import LoginScreen from "@/components/screens/Login/Login";
import Head from "next/head";

const Login = () => {
    return (
        <>
            <Head>
                <title>Login | Kleene Cars Admin</title>
            </Head>
            <LoginScreen />
        </>
    );
};

export default Login;
